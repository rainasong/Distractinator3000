const numberOfVideosToWatch = 1;
const progressBar = document.querySelector(".ytp-progress-bar");

function youtubeView() {
  window.setInterval(checkIfFinished, 1000);
}

function checkIfFinished() {}
